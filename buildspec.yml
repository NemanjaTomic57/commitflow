---
version: 0.2

phases:
  install:
    runtime-versions:
      golang: 1.26

  build:
    commands:
      - test -z "$(gofmt -l .)"
      - go vet ./...
      - golangci-lint run
      - yamllint .
    finally:
      - cat /etc/os-release
